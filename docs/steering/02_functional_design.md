# 機能設計書

**バージョン**: 1.2
**作成日**: 2025年12月29日
**最終更新**: 2026年2月7日

---

## 1. 機能一覧

### 1.1 機能カテゴリ

| カテゴリ | 説明 | 詳細ドキュメント |
|---------|------|-----------------|
| 接続管理 | DB接続の登録・編集・削除・起動 | [features/connection.md](features/connection.md) |
| クエリビルダー | GUIによるSQL構築・実行・結果表示 | [features/query-builder.md](features/query-builder.md) |
| SQLエディタ | フリーフォームSQL入力・実行・コード補完 | [features/sql-editor.md](features/sql-editor.md) |
| Mutation Builder | GUIによるINSERT/UPDATE/DELETE構築 | - |
| セキュリティ | 認証情報の暗号化・プロバイダー管理 | [features/security.md](features/security.md) |
| 設定 | アプリケーション設定・安全設定管理 | [features/settings.md](features/settings.md) |
| ウィンドウ管理 | マルチウィンドウ・状態保存 | [features/window.md](features/window.md) |

---

## 2. 画面一覧

### 2.1 画面構成

| 画面ID | 画面名 | URL/ルート | 説明 |
|--------|--------|-----------|------|
| S001 | ランチャー（ホーム） | `/` | 保存済み接続一覧と新規接続作成 |
| S002 | 接続設定画面 | `/connection-form` | データベース接続情報の設定 |
| S003 | クエリビルダー画面 | `/query-builder` | GUIでのSELECTクエリ構築・実行・結果表示 |
| S004 | 設定画面 | `/settings` | アプリケーション設定・安全設定 |
| S005 | Mutation Builder画面 | `/mutation-builder` | GUIでのINSERT/UPDATE/DELETE構築・実行 |
| S006 | SQLエディタ画面 | `/sql-editor` | フリーフォームSQL入力・実行 |

### 2.2 画面遷移図

```
┌─────────────────────────────────────────────────────────────┐
│                    アプリケーション起動                       │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│               ランチャー画面 [S001]                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  接続カード一覧                                      │   │
│  │  [開発DB] [テストDB] [本番DB] ...                   │   │
│  └─────────────────────────────────────────────────────┘   │
│  [新規作成ボタン]                    [設定ボタン]           │
└──────────┬──────────────────────────────┬───────────────────┘
           │                              │
           ▼                              ▼
┌────────────────────┐          ┌────────────────────┐
│ 接続設定画面 [S002] │          │  設定画面 [S004]    │
│ - 新規作成          │          │ - 一般設定          │
│ - 編集              │          │ - セキュリティ設定   │
│ - 接続テスト        │          │ - バージョン情報     │
└────────────────────┘          └────────────────────┘
           │
           │ 接続カードクリック
           ▼
┌─────────────────────────────────────────────────────────────┐
│          クエリビルダー画面 [S003]（別ウィンドウ）            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 環境識別ヘッダー（環境色・接続名・警告バナー）        │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ ツールバー（実行・保存・履歴）                       │   │
│  ├───────┬─────────────────────┬───────────────────────┤   │
│  │左パネル│   中央パネル        │    右パネル           │   │
│  │DB構造  │ テーブル関係図     │  SQLプレビュー        │   │
│  │ツリー  │ 条件設定タブ       │  クエリ情報           │   │
│  └───────┴─────────────────────┴───────────────────────┘   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│          SQLエディタ画面 [S006]（別ウィンドウ）               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 環境識別ヘッダー（環境色・接続名・警告バナー）        │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ ツールバー（実行・停止・整形・保存・DB/スキーマ選択）  │   │
│  ├───────────┬─────────────────────────────────────────┤   │
│  │ 左パネル   │          エディタエリア                  │   │
│  │ 保存クエリ │  タブバー [Tab1] [Tab2] [+]             │   │
│  │ ツリー     │  Monaco Editor                         │   │
│  │            │  (シンタックスハイライト・コード補完)     │   │
│  │ クエリ履歴 ├─────────────────────────────────────────┤   │
│  │            │          結果パネル                      │   │
│  │            │  実行結果テーブル / エラー表示            │   │
│  └───────────┴─────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 機能詳細サマリー

### 3.1 接続管理機能

**状態**: ✅ 完了（Phase 1.3）

| 機能 | 説明 | 対応コンポーネント |
|------|------|------------------|
| 接続一覧表示 | 登録済み接続をカード形式で表示 | `ConnectionList.vue`, `ConnectionCard.vue` |
| 接続作成 | 新規接続情報の登録 | `connection-form.vue` |
| 接続編集 | 既存接続情報の編集 | `connection-form.vue` |
| 接続削除 | 接続情報の削除 | `ConnectionCard.vue` |
| 接続テスト | DB接続の疎通確認 | `connection-form.vue` |
| 検索・フィルター | 名前/環境でフィルタリング | `SearchFilter.vue` |

### 3.2 クエリビルダー機能

**状態**: ✅ 完了（Phase 1〜5）

| 機能 | 説明 | 対応コンポーネント |
|------|------|------------------|
| データベースツリー | スキーマ/テーブル/カラムの階層表示 | `DatabaseTree.vue` |
| テーブル選択 | ドラッグ&ドロップでテーブル選択 | `TableCard.vue`, `DropZone.vue` |
| カラム選択 | チェックボックスでSELECT項目選択 | `SelectTab.vue`, `ColumnCheckbox.vue` |
| WHERE条件設定 | 条件行/グループ/ネスト対応 | `WhereTab.vue`, `ConditionRow.vue` |
| GROUP BY設定 | グループ化カラム選択 | `GroupByTab.vue` |
| ORDER BY設定 | ソート順設定（ASC/DESC） | `OrderByTab.vue` |
| LIMIT設定 | 件数制限とOFFSET | `LimitTab.vue` |
| SQLプレビュー | リアルタイムSQL生成 | `SqlPreview.vue` |
| クエリ実行 | SELECTクエリの実行 | `ResultPanel.vue` |
| 結果表示 | 実行結果の表形式表示 | `ResultTable.vue` |
| ページネーション | 結果のページ分割表示 | `ResultPagination.vue` |
| クエリ保存 | クエリの保存・読み込み | `SaveQueryDialog.vue`, `SavedQuerySlideover.vue` |
| クエリ履歴 | 実行済みクエリの履歴表示 | `QueryHistorySlideover.vue` |
| エクスポート | CSV/Excel/JSON形式での出力 | `ExportDialog.vue` |
| JOIN提案 | 外部キーに基づくJOIN提案 | `JoinSuggestionList.vue` |
| 危険クエリ確認 | UPDATE/DELETE時の確認ダイアログ | `DangerousQueryDialog.vue` |

### 3.3 SQLエディタ機能

**状態**: ✅ 完了（Phase 7）

| 機能 | 説明 | 対応コンポーネント |
|------|------|------------------|
| SQLテキストエディタ | Monaco Editorベースのフリーフォームエディタ | `SqlTextEditor.vue` |
| マルチタブ | 複数のSQLタブを同時に開いて作業 | `EditorTabs.vue` |
| シンタックスハイライト | SQL構文のカラーハイライト | `SqlTextEditor.vue` |
| コード補完 | テーブル名・カラム名・キーワード・関数の自動補完 | `useSqlCompletion.ts` |
| SQLフォーマッター | SQL文の自動整形 | `sql-formatter` |
| クエリ実行 | 選択範囲または全文の実行 | `SqlEditorToolbar.vue` |
| 結果表示 | 実行結果の表形式表示・エクスポート | `SqlEditorResultPanel.vue` |
| 保存クエリ管理 | フォルダ構造での保存・検索・移動 | `SqlEditorSavedPanel.vue` |
| クエリ履歴 | 実行済みクエリの履歴・検索 | `SqlEditorHistoryPanel.vue` |
| データベース/スキーマ切替 | 実行対象のDB・スキーマを切替 | `SqlEditorToolbar.vue` |
| エラーハイライト | エラー行・列のハイライト表示 | `SqlTextEditor.vue` |
| キーボードショートカット | Ctrl+Enter（実行）、Ctrl+S（保存）、Ctrl+Shift+F（整形） | `SqlTextEditor.vue` |
| 段階的DB構造取得 | 大量テーブルでの補完パフォーマンス最適化 | `database-structure.ts` |

### 3.5 Mutation Builder機能

**状態**: ✅ 完了（Phase 6）

| 機能 | 説明 | 対応コンポーネント |
|------|------|------------------|
| テーブル選択 | 対象テーブルの選択 | `TableSelector.vue` |
| INSERT構築 | フォーム入力によるINSERT文構築 | `InsertInputPanel.vue`, `InsertRowForm.vue` |
| UPDATE構築 | SET句とWHERE条件の設定 | `UpdatePanel.vue`, `SetTab.vue` |
| DELETE構築 | WHERE条件の設定 | `DeletePanel.vue`, `MutationWhereTab.vue` |
| SQLプレビュー | 生成されたSQLのプレビュー | `SqlPreviewPanel.vue` |
| クエリ実行 | Mutation実行と結果表示 | `MutationBuilderToolbar.vue` |

### 3.5 セキュリティ機能

**状態**: ✅ 完了（Phase 1.5a）

| 機能 | 説明 | 対応コンポーネント |
|------|------|------------------|
| Simpleプロバイダー | アプリ固定キーで暗号化 | `SecuritySettings.vue` |
| MasterPasswordプロバイダー | ユーザー設定パスワードで暗号化 | `MasterPasswordSetupDialog.vue` |
| Keychainプロバイダー | OSセキュアストレージ使用 | `SecuritySettings.vue` |
| プロバイダー切替 | プロバイダー間の移行 | `FromSimpleDialog.vue`, `FromMasterPasswordDialog.vue` |

### 3.6 ウィンドウ管理機能

**状態**: ✅ 完了（Phase 1.5）

| 機能 | 説明 | 対応コンポーネント |
|------|------|------------------|
| マルチウィンドウ | 複数接続を別ウィンドウで起動 | `WindowManager.rs` |
| ウィンドウ状態保存 | 位置・サイズの記憶 | `window_state.rs` |
| 環境識別ヘッダー | 環境色での視覚的識別 | `EnvironmentHeader.vue` |
| 環境警告バナー | 本番環境での警告表示 | `EnvironmentWarningBanner.vue` |

---

## 4. ユーザーストーリー

### 4.1 接続管理

```gherkin
Feature: 接続管理
  データベース接続情報を管理する

  Scenario: 新規接続を作成する
    Given ランチャー画面を開いている
    When 「新規接続」ボタンをクリックする
    And 接続情報（名前、ホスト、ポート等）を入力する
    And 「保存」ボタンをクリックする
    Then 接続カードが一覧に追加される

  Scenario: 接続をテストする
    Given 接続設定画面で接続情報を入力している
    When 「接続テスト」ボタンをクリックする
    Then 接続成功/失敗のメッセージが表示される

  Scenario: 接続を起動する
    Given ランチャー画面で接続カードが表示されている
    When 接続カードの「接続」ボタンをクリックする
    Then 新しいウィンドウでクエリビルダーが開く
    And 環境に応じた色でヘッダーが表示される
```

### 4.2 クエリ構築

```gherkin
Feature: クエリ構築
  GUIでSQLクエリを構築する

  Scenario: テーブルを選択する
    Given クエリビルダー画面を開いている
    When 左パネルのデータベースツリーからテーブルをドラッグする
    And 中央パネルのドロップゾーンにドロップする
    Then テーブルカードが表示される
    And SQLプレビューにFROM句が追加される

  Scenario: カラムを選択する
    Given テーブルが選択されている
    When 「SELECT」タブでカラムのチェックボックスをオンにする
    Then 選択カラムリストに追加される
    And SQLプレビューにカラム名が追加される

  Scenario: WHERE条件を設定する
    Given テーブルとカラムが選択されている
    When 「WHERE」タブで条件行を追加する
    And カラム、演算子、値を設定する
    Then SQLプレビューにWHERE句が追加される

  Scenario: 複雑な条件グループを作成する
    Given WHERE条件が設定されている
    When 「グループ追加」ボタンをクリックする
    And グループ内に複数の条件を追加する
    Then ネストした条件グループがSQLに反映される
```

### 4.3 セキュリティ

```gherkin
Feature: セキュリティプロバイダー
  認証情報の保存方法を管理する

  Scenario: MasterPasswordに切り替える
    Given Simpleプロバイダーを使用している
    When 設定画面でMasterPasswordを選択する
    And マスターパスワードを設定する
    Then 認証情報が再暗号化される
    And 次回起動時にパスワード入力が求められる

  Scenario: マスターパスワードを入力する
    Given MasterPasswordプロバイダーを使用している
    And アプリを起動する
    When パスワード入力ダイアログが表示される
    And 正しいパスワードを入力する
    Then アプリケーションが利用可能になる
```

---

## 5. ワイヤーフレーム

### 5.1 ランチャー画面

```
┌─────────────────────────────────────────────────────────────┐
│  SQL Query Builder                    [⚙️設定] [➕新規作成] │
├─────────────────────────────────────────────────────────────┤
│  🔍 検索...           [環境: すべて ▼]                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │ 🟢 開発DB   │  │ 🔵 テストDB │  │ 🟠 STG DB   │        │
│  │ PostgreSQL │  │ MySQL       │  │ PostgreSQL │        │
│  │ localhost  │  │ test-server │  │ stg-server │        │
│  │            │  │             │  │            │        │
│  │ [接続]     │  │ [接続]      │  │ [接続]     │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
│                                                             │
│  ┌─────────────┐                                           │
│  │ 🔴 本番DB   │                                           │
│  │ PostgreSQL │                                           │
│  │ prod-server│                                           │
│  │            │                                           │
│  │ [接続]     │                                           │
│  └─────────────┘                                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 クエリビルダー画面

```
┌─────────────────────────────────────────────────────────────┐
│ 🔴 本番環境 - prod-server                            [✕]   │
│ ⚠️ 本番環境です。操作には十分注意してください。              │
├─────────────────────────────────────────────────────────────┤
│ [▶実行] [💾保存] [📂開く] [📜履歴]      [クエリ: 未保存]   │
├───────────┬─────────────────────────┬───────────────────────┤
│ DBツリー   │   テーブル関係エリア   │   SQLプレビュー        │
│           │                         │                       │
│ 📁public  │  ┌──────────┐           │   SELECT              │
│  └📋users │  │  users   │           │     u.id,             │
│    ├ id   │  │ (u)      │           │     u.name,           │
│    ├ name │  │ ──────── │           │     u.email           │
│    └ email│  │ id       │           │   FROM                │
│  └📋orders│  │ name     │           │     public.users u    │
│    ├ id   │  │ email    │           │   WHERE               │
│    └ total│  └──────────┘           │     u.id > 10         │
│           │                         │                       │
│           ├─────────────────────────┤                       │
│           │ [SELECT][WHERE][GROUP]  │                       │
│           │ [ORDER][LIMIT]          │   ─────────────────   │
│           │                         │   クエリ情報          │
│           │ WHERE条件設定           │   テーブル数: 1       │
│           │ ┌───────────────────┐  │   カラム数: 3         │
│           │ │ u.id > [10    ]  │   │   条件数: 1           │
│           │ │ [➕条件追加]      │   │                       │
│           │ └───────────────────┘  │                       │
├───────────┴─────────────────────────┴───────────────────────┤
│ 実行結果エリア                                               │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ id │ name   │ email          │ created_at            │   │
│ │────┼────────┼────────────────┼───────────────────────│   │
│ │ 11 │ user11 │ user11@ex.com  │ 2025-01-01 10:00:00   │   │
│ │ 12 │ user12 │ user12@ex.com  │ 2025-01-02 11:00:00   │   │
│ └───────────────────────────────────────────────────────┘   │
│ [< 前へ] ページ 1/10 [次へ >]    [CSV] [Excel] [JSON]       │
└─────────────────────────────────────────────────────────────┘
```

### 5.3 設定画面

```
┌─────────────────────────────────────────────────────────────┐
│  ← 戻る                設定                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [一般] [セキュリティ] [バージョン情報]                      │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  セキュリティ設定                                            │
│                                                             │
│  認証情報の保存方法                                          │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ○ Simple（デフォルト）                              │   │
│  │   アプリ固定のキーで暗号化                          │   │
│  │   セキュリティレベル: 低                            │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ ● マスターパスワード                                │   │
│  │   ユーザー設定のパスワードで暗号化                  │   │
│  │   セキュリティレベル: 高                            │   │
│  │   [パスワード変更]                                  │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ ○ OSキーチェーン                                   │   │
│  │   OSのセキュアストレージを使用                      │   │
│  │   セキュリティレベル: 最高                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 詳細仕様へのリンク

各機能の詳細仕様は以下のサブディレクトリを参照してください。

| ドキュメント | 内容 |
|------------|------|
| [features/connection.md](features/connection.md) | 接続管理機能の詳細仕様 |
| [features/query-builder.md](features/query-builder.md) | クエリビルダー機能の詳細仕様 |
| [features/sql-editor.md](features/sql-editor.md) | SQLエディタ機能の詳細仕様 |
| [features/security.md](features/security.md) | セキュリティ機能の詳細仕様 |
| [features/settings.md](features/settings.md) | 設定機能の詳細仕様 |
| [features/window.md](features/window.md) | ウィンドウ管理機能の詳細仕様 |

---

## 変更履歴

| 日付 | バージョン | 変更内容 | 作成者 |
|------|----------|---------|--------|
| 2025-12-29 | 1.0 | 初版作成 | - |
| 2026-01-17 | 1.1 | Mutation Builder機能追加、クエリビルダー機能の実装完了を反映、画面一覧更新 | - |
| 2026-02-07 | 1.2 | SQLエディタ機能（Phase 7）追加、画面一覧・機能カテゴリ・画面遷移図を更新 | - |
