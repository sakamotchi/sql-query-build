# タスク1.5b.2.4.1: pages/index.vue作成

**サブフェーズ**: 2.4 ランチャーページ
**工数**: 4時間
**優先度**: 高
**依存関係**: 2.2.1 (connectionストア), 2.3.1 (EnvironmentHeader), 2.4.2 (ConnectionCard)

---

## 目的

ランチャーページ（接続一覧画面）を実装

---

## 実装内容

### ファイル作成: `app/pages/index.vue`

元の設計書の720-824行目を参照してください。

**主要機能**:
1. 接続一覧の表示
2. 検索機能
3. 環境フィルター
4. 新規接続ボタン
5. ローディング表示
6. 空状態の表示

---

## 確認事項

- [ ] ページが作成されたか
- [ ] 接続一覧が表示されるか
- [ ] 検索・フィルターが動作するか
- [ ] 新規接続ボタンが動作するか
- [ ] ローディング状態が表示されるか
- [ ] 空状態が表示されるか

---

## 成果物

- `app/pages/index.vue`

---

## ページ構成

### 1. ヘッダー
- EnvironmentHeaderコンポーネント
- 現在の環境表示
- テーマ切り替えボタン

### 2. ツールバー
- ページタイトル「接続一覧」
- 新規接続ボタン

### 3. 検索・フィルターエリア
- 検索入力フィールド（接続名、ホスト、DB名で検索）
- 環境フィルタードロップダウン

### 4. 接続リスト
- グリッドレイアウト（レスポンシブ）
- ConnectionCardコンポーネント
- ローディング表示
- 空状態表示

---

## 状態管理

```typescript
const searchQuery = ref('')
const selectedEnvironmentFilter = ref<string>('all')

const filteredConnections = computed(() => {
  // 環境フィルター + 検索クエリでフィルタリング
})
```

---

## Computed Properties

| 名称 | 説明 |
|------|------|
| `filteredConnections` | 検索クエリと環境フィルターを適用した接続リスト |

---

## メソッド

| 名称 | 説明 |
|------|------|
| `navigateToConnectionForm()` | 新規接続フォームへ遷移 |

---

## 使用コンポーネント

- `<EnvironmentHeader>`: 環境ヘッダー
- `<ConnectionCard>`: 接続カード
- `<UInput>`: 検索入力
- `<USelect>`: 環境フィルター
- `<UButton>`: 新規接続ボタン
- `<UIcon>`: アイコン

---

## レスポンシブデザイン

- モバイル: 1列
- タブレット: 2列
- デスクトップ: 3列

```vue
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
```

---

## 次のタスク

→ [2.4.2: ConnectionCard.vue作成](2.4.2_ConnectionCard.md)
