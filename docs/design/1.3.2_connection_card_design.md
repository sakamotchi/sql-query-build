# タスク1.3.2: 接続カードコンポーネント作成 設計書

## 概要

**タスクID**: 1.3.2
**タスク名**: 接続カードコンポーネント作成
**工数**: 1日
**依存関係**: 1.3.1 (起動画面(ランチャー)UI実装)
**完了条件**: カードが表示される

---

## 目的

ランチャー画面で各データベース接続を視覚的に表示するカードコンポーネントを実装する。環境別の色分け、接続情報の表示、各種操作ボタンを提供する。

---

## コンポーネント設計

### コンポーネント: `ConnectionCard.vue`

**配置場所**: `src/components/connection/ConnectionCard.vue`

---

## カードレイアウト

### ビジュアルデザイン

```
┌─────────────────────────────────┐
│ ┌─ 環境色バー (4px) ───────────┐│
│ │                              ││
│ └──────────────────────────────┘│
│                                 │
│  [環境アイコン] 接続名           │
│  開発環境                        │
│                                 │
│  📊 PostgreSQL                  │
│  🖥️  localhost:5432             │
│  💾 database_name               │
│                                 │
│  ─────────────────────────────  │
│                                 │
│  最終使用: 2025-10-05 14:30     │
│                                 │
│  [接続] [編集] [︙]              │
└─────────────────────────────────┘
```

---

## Props定義

```typescript
interface ConnectionCardProps {
  connection: Connection;
}

interface Connection {
  id: string;
  name: string;
  environment: Environment;
  themeColor: string;
  host: string;
  port: number;
  database: string;
  username: string;
  dbType: DatabaseType;
  ssl: boolean;
  createdAt: string;
  updatedAt: string;
  lastUsedAt?: string;
}

type Environment = 'development' | 'test' | 'staging' | 'production';
type DatabaseType = 'postgresql' | 'mysql' | 'sqlite';
```

---

## Emits定義

```typescript
interface ConnectionCardEmits {
  (e: 'select', connection: Connection): void;   // 接続ボタンクリック
  (e: 'edit', connection: Connection): void;     // 編集ボタンクリック
  (e: 'delete', connection: Connection): void;   // 削除メニュー選択
  (e: 'duplicate', connection: Connection): void; // 複製メニュー選択
}
```

---

## コンポーネント実装

### テンプレート構造

```vue
<template>
  <v-card
    :class="['connection-card', `environment-${connection.environment}`]"
    :style="{ borderTopColor: environmentColor }"
    elevation="2"
    hover
    @click="handleCardClick"
  >
    <!-- 環境色バー -->
    <div class="environment-bar" :style="{ backgroundColor: environmentColor }"></div>

    <v-card-title class="d-flex align-center">
      <!-- 環境アイコン -->
      <v-icon :color="environmentColor" size="small" class="mr-2">
        {{ environmentIcon }}
      </v-icon>

      <!-- 接続名 -->
      <span class="text-h6">{{ connection.name }}</span>

      <v-spacer></v-spacer>

      <!-- メニューボタン -->
      <v-menu>
        <template v-slot:activator="{ props }">
          <v-btn
            icon="mdi-dots-vertical"
            size="small"
            variant="text"
            v-bind="props"
            @click.stop
          ></v-btn>
        </template>

        <v-list>
          <v-list-item @click="$emit('edit', connection)">
            <template v-slot:prepend>
              <v-icon>mdi-pencil</v-icon>
            </template>
            <v-list-item-title>編集</v-list-item-title>
          </v-list-item>

          <v-list-item @click="$emit('duplicate', connection)">
            <template v-slot:prepend>
              <v-icon>mdi-content-copy</v-icon>
            </template>
            <v-list-item-title>複製</v-list-item-title>
          </v-list-item>

          <v-divider></v-divider>

          <v-list-item @click="$emit('delete', connection)" class="text-error">
            <template v-slot:prepend>
              <v-icon color="error">mdi-delete</v-icon>
            </template>
            <v-list-item-title>削除</v-list-item-title>
          </v-list-item>
        </v-list>
      </v-menu>
    </v-card-title>

    <v-card-subtitle>
      <v-chip
        :color="environmentColor"
        size="small"
        variant="tonal"
      >
        {{ environmentLabel }}
      </v-chip>
    </v-card-subtitle>

    <v-card-text>
      <!-- データベース情報 -->
      <div class="connection-info">
        <div class="info-row">
          <v-icon size="small" class="mr-2">{{ databaseIcon }}</v-icon>
          <span class="text-body-2">{{ databaseTypeLabel }}</span>
          <v-chip v-if="connection.ssl" size="x-small" color="success" class="ml-2">
            SSL
          </v-chip>
        </div>

        <div class="info-row">
          <v-icon size="small" class="mr-2">mdi-server</v-icon>
          <span class="text-body-2">{{ connection.host }}:{{ connection.port }}</span>
        </div>

        <div class="info-row">
          <v-icon size="small" class="mr-2">mdi-database</v-icon>
          <span class="text-body-2">{{ connection.database }}</span>
        </div>

        <div class="info-row">
          <v-icon size="small" class="mr-2">mdi-account</v-icon>
          <span class="text-body-2">{{ connection.username }}</span>
        </div>
      </div>

      <v-divider class="my-3"></v-divider>

      <!-- 最終使用日時 -->
      <div class="text-caption text-grey">
        <v-icon size="x-small" class="mr-1">mdi-clock-outline</v-icon>
        {{ lastUsedText }}
      </div>
    </v-card-text>

    <v-card-actions>
      <v-btn
        color="primary"
        variant="tonal"
        prepend-icon="mdi-database-arrow-right"
        @click.stop="$emit('select', connection)"
        block
      >
        接続
      </v-btn>
    </v-card-actions>
  </v-card>
</template>
```

---

## スクリプト実装

```typescript
<script setup lang="ts">
import { computed } from 'vue';
import type { Connection, Environment, DatabaseType } from '@/types/connection';

const props = defineProps<{
  connection: Connection;
}>();

const emit = defineEmits<{
  select: [connection: Connection];
  edit: [connection: Connection];
  delete: [connection: Connection];
  duplicate: [connection: Connection];
}>();

// 環境別の色定義
const environmentColors: Record<Environment, string> = {
  development: '#4CAF50',  // 緑
  test: '#2196F3',         // 青
  staging: '#FF9800',      // オレンジ
  production: '#F44336',   // 赤
};

// 環境別のラベル
const environmentLabels: Record<Environment, string> = {
  development: '開発環境',
  test: 'テスト環境',
  staging: 'ステージング環境',
  production: '本番環境',
};

// 環境別のアイコン
const environmentIcons: Record<Environment, string> = {
  development: 'mdi-laptop',
  test: 'mdi-flask',
  staging: 'mdi-server',
  production: 'mdi-alert-circle',
};

// データベース種別のアイコン
const databaseIcons: Record<DatabaseType, string> = {
  postgresql: 'mdi-elephant',
  mysql: 'mdi-dolphin',
  sqlite: 'mdi-database',
};

// データベース種別のラベル
const databaseTypeLabels: Record<DatabaseType, string> = {
  postgresql: 'PostgreSQL',
  mysql: 'MySQL',
  sqlite: 'SQLite',
};

// 計算プロパティ
const environmentColor = computed(() =>
  environmentColors[props.connection.environment]
);

const environmentLabel = computed(() =>
  environmentLabels[props.connection.environment]
);

const environmentIcon = computed(() =>
  environmentIcons[props.connection.environment]
);

const databaseIcon = computed(() =>
  databaseIcons[props.connection.dbType]
);

const databaseTypeLabel = computed(() =>
  databaseTypeLabels[props.connection.dbType]
);

const lastUsedText = computed(() => {
  if (!props.connection.lastUsedAt) {
    return '未使用';
  }

  const lastUsed = new Date(props.connection.lastUsedAt);
  const now = new Date();
  const diffMs = now.getTime() - lastUsed.getTime();
  const diffMins = Math.floor(diffMs / 60000);
  const diffHours = Math.floor(diffMs / 3600000);
  const diffDays = Math.floor(diffMs / 86400000);

  if (diffMins < 1) return 'たった今';
  if (diffMins < 60) return `${diffMins}分前`;
  if (diffHours < 24) return `${diffHours}時間前`;
  if (diffDays < 7) return `${diffDays}日前`;

  return lastUsed.toLocaleDateString('ja-JP', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit',
  });
});

// カードクリック時の処理 (カード全体をクリック可能に)
const handleCardClick = () => {
  emit('select', props.connection);
};
</script>
```

---

## スタイル定義

```scss
<style scoped lang="scss">
.connection-card {
  position: relative;
  border-top: 4px solid transparent;
  transition: all 0.3s ease;
  cursor: pointer;

  &:hover {
    transform: translateY(-4px);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15) !important;
  }

  &.environment-production {
    .v-card-title {
      font-weight: 600;
    }
  }
}

.environment-bar {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  border-radius: 4px 4px 0 0;
}

.connection-info {
  .info-row {
    display: flex;
    align-items: center;
    margin-bottom: 8px;

    &:last-child {
      margin-bottom: 0;
    }

    .v-icon {
      opacity: 0.7;
    }
  }
}

// 環境別のカードスタイル
.environment-development {
  .v-card-title {
    color: #2E7D32;
  }
}

.environment-test {
  .v-card-title {
    color: #1565C0;
  }
}

.environment-staging {
  .v-card-title {
    color: #E65100;
  }
}

.environment-production {
  .v-card-title {
    color: #C62828;
  }

  border: 2px solid rgba(244, 67, 54, 0.3);

  &:hover {
    border-color: rgba(244, 67, 54, 0.6);
  }
}
</style>
```

---

## 環境別の視覚的特徴

### 開発環境 (Development)
- **色**: 緑 (#4CAF50)
- **アイコン**: ノートパソコン (mdi-laptop)
- **特徴**: 安全な作業環境を示す

### テスト環境 (Test)
- **色**: 青 (#2196F3)
- **アイコン**: フラスコ (mdi-flask)
- **特徴**: 検証環境を示す

### ステージング環境 (Staging)
- **色**: オレンジ (#FF9800)
- **アイコン**: サーバー (mdi-server)
- **特徴**: 注意が必要な環境

### 本番環境 (Production)
- **色**: 赤 (#F44336)
- **アイコン**: 警告 (mdi-alert-circle)
- **特徴**: 危険を示す赤、太枠、強調表示

---

## インタラクション

### ホバー効果
- カードが浮き上がる (translateY: -4px)
- シャドウが濃くなる
- 本番環境の場合、ボーダーカラーが濃くなる

### クリック動作
- **カード全体クリック**: 接続を選択して新規ウィンドウで起動
- **接続ボタンクリック**: 接続を選択して新規ウィンドウで起動
- **メニューボタンクリック**: ドロップダウンメニュー表示

### メニューオプション
1. **編集**: 接続設定画面を開く
2. **複製**: 接続情報を複製して新規作成画面を開く
3. **削除**: 削除確認ダイアログを表示

---

## アクセシビリティ

### ARIA属性

```vue
<v-card
  :aria-label="`${connection.name}への接続。${environmentLabel}。${databaseTypeLabel}。${connection.host}`"
  role="button"
  tabindex="0"
  @keydown.enter="handleCardClick"
  @keydown.space.prevent="handleCardClick"
>
```

### キーボード操作
- **Enter / Space**: カードを選択して接続
- **Tab**: 次のカードへフォーカス移動
- **Shift + Tab**: 前のカードへフォーカス移動

---

## レスポンシブデザイン

### ブレークポイント別カードサイズ

```scss
// xs (< 600px): 1カラム - カード幅100%
@media (max-width: 599px) {
  .connection-card {
    .v-card-title {
      font-size: 1rem;
    }
  }
}

// sm (≥ 600px): 2カラム - カード幅50%
// md (≥ 960px): 3カラム - カード幅33.3%
// lg (≥ 1280px): 4カラム - カード幅25%

// 最小カード高さ
.connection-card {
  min-height: 280px;
}
```

---

## パフォーマンス最適化

### 仮想スクロール対応
- 接続が100件を超える場合、親コンポーネントで`v-virtual-scroll`を使用

### メモ化
- 環境色、ラベル、アイコンは`computed`プロパティで計算
- 最終使用日時の相対表示は`computed`でキャッシュ

---

## エラーハンドリング

### デフォルト値
```typescript
const connection = computed(() => ({
  ...props.connection,
  lastUsedAt: props.connection.lastUsedAt || null,
  ssl: props.connection.ssl ?? false,
}));
```

---

## テスト観点

### ユニットテスト
- [ ] 環境別の色が正しく表示されること
- [ ] データベース種別のアイコンが正しく表示されること
- [ ] 最終使用日時の相対表示が正しいこと
- [ ] SSLバッジが正しく表示されること
- [ ] イベントが正しく発火すること

### ビジュアルリグレッションテスト
- [ ] 4つの環境すべてでカードが正しく表示されること
- [ ] ホバー状態が正しく表示されること
- [ ] レスポンシブデザインが正しいこと

### E2Eテスト
- [ ] カードクリックで接続が選択されること
- [ ] 編集ボタンで編集画面に遷移すること
- [ ] 削除ボタンで削除確認ダイアログが表示されること

---

## 実装チェックリスト

- [ ] `src/components/connection/ConnectionCard.vue` の作成
- [ ] 環境別の色定義を実装
- [ ] データベース種別のアイコン定義を実装
- [ ] 最終使用日時の相対表示を実装
- [ ] ホバー効果の実装
- [ ] メニューの実装
- [ ] アクセシビリティ対応
- [ ] レスポンシブデザインの確認
- [ ] ユニットテストの作成
- [ ] ビジュアルリグレッションテストの作成

---

## デザイントークン

### 色定義
```typescript
export const EnvironmentColors = {
  development: {
    primary: '#4CAF50',
    light: '#81C784',
    dark: '#388E3C',
    background: '#F1F8E9',
  },
  test: {
    primary: '#2196F3',
    light: '#64B5F6',
    dark: '#1976D2',
    background: '#E3F2FD',
  },
  staging: {
    primary: '#FF9800',
    light: '#FFB74D',
    dark: '#F57C00',
    background: '#FFF3E0',
  },
  production: {
    primary: '#F44336',
    light: '#E57373',
    dark: '#D32F2F',
    background: '#FFEBEE',
  },
};
```

### スペーシング
```scss
$card-padding: 16px;
$card-gap: 16px;
$environment-bar-height: 4px;
$card-border-radius: 4px;
$card-min-height: 280px;
```
