# タスクリスト：保存クエリフォルダ管理 - Phase1 データモデル設計

**作成日**: 2026-01-24
**バージョン**: 1.0
**ステータス**: 📝 計画中

---

## 進捗サマリー

| ステータス | タスク数 | 進捗率 |
|-----------|---------|--------|
| ✅ 完了 | 0 | 0% |
| 🚧 進行中 | 0 | 0% |
| ⏳ 未着手 | 10 | 100% |
| **合計** | **10** | **0%** |

---

## タスク一覧

### 1. TypeScript型定義の更新

**ファイル**: [app/types/saved-query.ts](../../app/types/saved-query.ts)

**目的**: フロントエンドの型定義に `folderPath` と `connectionId` の nullable 化を追加

**タスク**:
- [ ] 1.1 `SavedQuery` インターフェースに `folderPath: string | null` を追加
- [ ] 1.2 `SavedQuery` インターフェースの `connectionId` を `string | null` に変更
- [ ] 1.3 `SavedQueryMetadata` インターフェースに `folderPath: string | null` を追加
- [ ] 1.4 `SavedQueryMetadata` インターフェースの `connectionId` を `string | null` に変更
- [ ] 1.5 `SaveQueryRequest` インターフェースに `folderPath?: string | null` を追加
- [ ] 1.6 `SaveQueryRequest` インターフェースの `connectionId` を `string | null` に変更
- [ ] 1.7 `SearchQueryRequest` インターフェースに `folderPath?: string` を追加

**成果物**:
- 更新された [app/types/saved-query.ts](../../app/types/saved-query.ts)

**見積**: 1時間
**ステータス**: ⏳ 未着手

---

### 2. Rust構造体の更新

**ファイル**: [src-tauri/src/models/saved_query.rs](../../src-tauri/src/models/saved_query.rs)

**目的**: バックエンドの構造体に `folder_path` と `connection_id` の Option型を追加

**タスク**:
- [ ] 2.1 `SavedQuery` 構造体に `pub folder_path: Option<String>` を追加
- [ ] 2.2 `SavedQuery` 構造体の `connection_id` を `Option<String>` に変更
- [ ] 2.3 `SavedQueryMetadata` 構造体に `pub folder_path: Option<String>` を追加
- [ ] 2.4 `SavedQueryMetadata` 構造体の `connection_id` を `Option<String>` に変更
- [ ] 2.5 `SaveQueryRequest` 構造体に `pub folder_path: Option<String>` を追加
- [ ] 2.6 `SaveQueryRequest` 構造体の `connection_id` を `Option<String>` に変更
- [ ] 2.7 `SearchQueryRequest` 構造体に `pub folder_path: Option<String>` を追加

**成果物**:
- 更新された [src-tauri/src/models/saved_query.rs](../../src-tauri/src/models/saved_query.rs)

**見積**: 1時間
**ステータス**: ⏳ 未着手

---

### 3. TypeScriptバリデーション関数の実装

**ファイル**: [app/utils/folder-validation.ts](../../app/utils/folder-validation.ts)（新規作成）

**目的**: フォルダパスとフォルダ名のバリデーション関数を実装

**タスク**:
- [ ] 3.1 ファイル作成: `app/utils/folder-validation.ts`
- [ ] 3.2 `FolderPathValidationResult` インターフェースを定義
- [ ] 3.3 `validateFolderPath()` 関数を実装
  - null チェック
  - 空文字列チェック
  - 先頭・末尾スラッシュチェック
  - 最大階層チェック（10階層）
  - 禁止文字チェック
  - パストラバーサルチェック
- [ ] 3.4 `validateFolderName()` 関数を実装
- [ ] 3.5 `parseFolderPath()` 関数を実装
- [ ] 3.6 `buildFolderPath()` 関数を実装

**成果物**:
- [app/utils/folder-validation.ts](../../app/utils/folder-validation.ts)

**見積**: 2時間
**ステータス**: ⏳ 未着手

---

### 4. Rustバリデーション関数の実装

**ファイル**: [src-tauri/src/utils/folder_validation.rs](../../src-tauri/src/utils/folder_validation.rs)（新規作成）

**目的**: フォルダパスとフォルダ名のバリデーション関数を実装

**タスク**:
- [ ] 4.1 ファイル作成: `src-tauri/src/utils/folder_validation.rs`
- [ ] 4.2 `validate_folder_path()` 関数を実装
  - None チェック
  - 空文字列チェック
  - 先頭・末尾スラッシュチェック
  - 最大階層チェック（10階層）
  - 禁止文字チェック
  - パストラバーサルチェック
- [ ] 4.3 `validate_folder_name()` 関数を実装
- [ ] 4.4 `parse_folder_path()` 関数を実装
- [ ] 4.5 `build_folder_path()` 関数を実装
- [ ] 4.6 ユニットテストを実装（`#[cfg(test)] mod tests`）

**成果物**:
- [src-tauri/src/utils/folder_validation.rs](../../src-tauri/src/utils/folder_validation.rs)

**見積**: 2時間
**ステータス**: ⏳ 未着手

---

### 5. Rustモジュール登録

**ファイル**: [src-tauri/src/utils/mod.rs](../../src-tauri/src/utils/mod.rs)（新規作成または既存ファイルに追加）

**目的**: `folder_validation` モジュールを登録

**タスク**:
- [ ] 5.1 `src-tauri/src/utils/mod.rs` を確認（存在しない場合は新規作成）
- [ ] 5.2 `pub mod folder_validation;` を追加
- [ ] 5.3 `src-tauri/src/lib.rs` または `main.rs` で `mod utils;` を追加（必要に応じて）

**成果物**:
- 更新された [src-tauri/src/utils/mod.rs](../../src-tauri/src/utils/mod.rs)

**見積**: 0.5時間
**ステータス**: ⏳ 未着手

---

### 6. TypeScriptテストの実装

**ファイル**: [tests/utils/folder-validation.spec.ts](../../tests/utils/folder-validation.spec.ts)（新規作成）

**目的**: バリデーション関数のユニットテストを実装

**タスク**:
- [ ] 6.1 ファイル作成: `tests/utils/folder-validation.spec.ts`
- [ ] 6.2 `validateFolderPath()` のテストケースを実装
  - 正常系: null、正しいパス形式
  - 異常系: 先頭スラッシュなし、末尾スラッシュあり、禁止文字、最大階層超え、パストラバーサル
- [ ] 6.3 `validateFolderName()` のテストケースを実装
  - 正常系: 正しいフォルダ名
  - 異常系: 空文字列、最大長超え、禁止文字
- [ ] 6.4 `parseFolderPath()` のテストケースを実装
- [ ] 6.5 `buildFolderPath()` のテストケースを実装
- [ ] 6.6 テストを実行: `npm run test tests/utils/folder-validation.spec.ts`

**成果物**:
- [tests/utils/folder-validation.spec.ts](../../tests/utils/folder-validation.spec.ts)

**見積**: 1.5時間
**ステータス**: ⏳ 未着手

---

### 7. 既存コードの影響確認と修正

**目的**: 型定義の変更による既存コードへの影響を確認し、必要に応じて修正

**タスク**:
- [ ] 7.1 TypeScript型チェックを実行: `npm run typecheck`
- [ ] 7.2 型エラーが発生した箇所を特定
- [ ] 7.3 `connectionId` が `string | null` になったことによる影響を修正
  - `app/stores/saved-query.ts`
  - `app/components/query-builder/SavedQuerySlideover.vue`
  - `app/components/query-builder/dialog/SaveQueryDialog.vue`
  - その他、SavedQuery型を使用している箇所
- [ ] 7.4 再度型チェックを実行し、エラーがないことを確認

**成果物**:
- 修正された既存ファイル（必要に応じて）

**見積**: 2時間
**ステータス**: ⏳ 未着手

---

### 8. Rustビルド確認と修正

**目的**: Rust構造体の変更によるビルドエラーを確認し、必要に応じて修正

**タスク**:
- [ ] 8.1 Rustビルドを実行: `cargo build --manifest-path=src-tauri/Cargo.toml`
- [ ] 8.2 ビルドエラーが発生した箇所を特定
- [ ] 8.3 `connection_id` が `Option<String>` になったことによる影響を修正
  - `src-tauri/src/commands/query_storage_commands.rs`
  - `src-tauri/src/services/query_storage.rs`
  - その他、SavedQuery構造体を使用している箇所
- [ ] 8.4 再度ビルドを実行し、エラーがないことを確認

**成果物**:
- 修正された既存ファイル（必要に応じて）

**見積**: 2時間
**ステータス**: ⏳ 未着手

---

### 9. 統合テスト

**目的**: 既存のクエリJSONファイルが正常に読み込まれることを確認

**タスク**:
- [ ] 9.1 既存の保存クエリJSONファイルを確認
- [ ] 9.2 アプリケーションを起動: `npm run tauri:dev`
- [ ] 9.3 保存クエリ一覧を開き、既存クエリが表示されることを確認
- [ ] 9.4 既存クエリを読み込み、正常に動作することを確認
- [ ] 9.5 新規クエリを保存し、`folderPath: null` が設定されることを確認
- [ ] 9.6 保存したクエリのJSONファイルを確認し、フィールドが正しく保存されていることを確認

**成果物**:
- テスト結果レポート（手動テスト）

**見積**: 1時間
**ステータス**: ⏳ 未着手

---

### 10. ドキュメント更新

**目的**: 永続化ドキュメントに今回の変更を反映

**タスク**:
- [ ] 10.1 `docs/steering/06_ubiquitous_language.md` を更新
  - 「フォルダパス」の用語を追加
- [ ] 10.2 `docs/steering/03_architecture_specifications.md` を更新（必要に応じて）
  - データモデルの変更を記載
- [ ] 10.3 Phase1完了のコミットメッセージを作成

**成果物**:
- 更新された永続化ドキュメント

**見積**: 1時間
**ステータス**: ⏳ 未着手

---

## タスクの依存関係

```
1. TypeScript型定義の更新
   ↓
3. TypeScriptバリデーション関数の実装
   ↓
6. TypeScriptテストの実装
   ↓
7. 既存コードの影響確認と修正

2. Rust構造体の更新
   ↓
4. Rustバリデーション関数の実装
   ↓
5. Rustモジュール登録
   ↓
8. Rustビルド確認と修正

7, 8 完了後
   ↓
9. 統合テスト
   ↓
10. ドキュメント更新
```

---

## マイルストーン

| マイルストーン | 完了条件 | 期限目安 |
|--------------|---------|---------|
| M1: 型定義更新完了 | タスク1, 2完了 | Day 1 |
| M2: バリデーション実装完了 | タスク3, 4, 5完了 | Day 2 |
| M3: テスト完了 | タスク6完了 | Day 2.5 |
| M4: 既存コード修正完了 | タスク7, 8完了 | Day 3 |
| M5: 統合テスト完了 | タスク9完了 | Day 3.5 |
| M6: ドキュメント更新完了 | タスク10完了 | Day 4 |

**総見積工数**: 14時間（約2日）

---

## 注意事項

### Phase1のスコープ

**Phase1に含まれるもの**:
- データモデルの設計と実装
- バリデーション関数の実装
- 既存データとの後方互換性確保

**Phase1に含まれないもの**:
- フォルダ操作API（`move_query`, `rename_folder`, `delete_folder`）→ Phase2
- ツリー構造のビルド処理 → Phase3
- UI実装（ツリービュー、フォルダ操作） → Phase4

### 後方互換性の確認

- **重要**: 既存のクエリJSONファイル（`folder_path`フィールドなし）が正常に読み込まれることを必ず確認してください
- Rustの`Option<T>`型により、フィールドが存在しない場合は自動的に`None`として扱われます
- **マイグレーション処理は不要**です

### TypeScriptとRustの型の対応

| TypeScript | Rust |
|------------|------|
| `string \| null` | `Option<String>` |
| `string[]` | `Vec<String>` |
| `{ ... }` | `serde_json::Value` |

### テストの実行

**TypeScriptテスト**:
```bash
npm run test tests/utils/folder-validation.spec.ts
```

**Rustテスト**:
```bash
cargo test --manifest-path=src-tauri/Cargo.toml folder_validation
```

**型チェック**:
```bash
npm run typecheck
```

**ビルド**:
```bash
npm run build
npm run tauri:build
```

---

## 変更履歴

| 日付 | バージョン | 変更内容 | 作成者 |
|------|----------|---------|--------|
| 2026-01-24 | 1.0 | 初版作成 | - |
